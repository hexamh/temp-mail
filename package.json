{
	"name": "agent",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler d1 execute temp --file=./schema.sql --remote && wrangler types && wrangler deploy",
		"start": "wrangler dev",
		"dev": "wrangler dev",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
	"postal-mime": "latest"
  },
	"devDependencies": {
		"@types/node": "^25.4.0",
		"typescript": "^5.0.4",
		"wrangler": "^4.34.0"
	}
}
